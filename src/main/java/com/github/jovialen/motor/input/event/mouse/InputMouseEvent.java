package com.github.jovialen.motor.input.event.mouse;

import com.github.jovialen.motor.input.event.InputEvent;
import com.github.jovialen.motor.window.Window;

public class InputMouseEvent extends InputEvent {
    public InputMouseEvent(Window window) {
        super(window);
    }
}
